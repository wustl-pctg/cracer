#include <stdio.h>

#include <cilk-lib.cilkh>

cilk void f(int x)
{
  x = 5;
  x++;
  spawn delayed_batchify();
  return;
  sync;
}

cilk int main(int argc, void* argv[])
{
  spawn f(42);
  sync;

  printf("%s\n", __func__);
  return 0;
}
