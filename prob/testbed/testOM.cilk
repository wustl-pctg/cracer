#include "order_maintenance.cilk"
#define TEST_SIZE 100	//Needs to fit with bound (see M)

int main()
{
	int test_ints[10] = {-1,0,1,2,3,4,5,6,7,8};
	itemOM* base = create_itemOM(&test_ints[0]);
	OM* testOM = create_OM(base);
	itemOM* item0 = creat_itemOM(&test_ints[1]);
	
	individual_insert(testOM, base, item0);
	
	print_OM(testOM);
	
	return 0;
}
